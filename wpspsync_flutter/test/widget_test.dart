import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — full integration tests will be added once services are mockable.
    expect(true, isTrue);
  });
}
